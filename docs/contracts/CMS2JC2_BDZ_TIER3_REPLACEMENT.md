# CMS2JC2 B_DZ tier3 replacement v1

Authority: `docs/plans/CMS2JC2_BDZ_TIER3_REPLACEMENT_PLAN.md`.
Additive CMS2JC2_RESPONSE v1 kinds: BDZ_TIER3, BDZ_TIER3_REUSE,
BDZ_TIER3_RETIREMENT. Original scientific contracts are unchanged.

The stage binds the new tier3 study, original debug comparison/ledger/journals,
original gate through parent_spec, acceptance and calibration receipt hashes,
unchanged protocol and exact five-task resource/dependency registry. Reuse
authenticates all existing scientific source, physical/data interface and
numerical environment. No original evaluation output or claim may exist.

Retirement binds new stage/plan and old ledger, exact five job IDs and
non-success terminal states. Explicit authorization and partition admission
precede exact pending-only controller cancellation. Active, successful or
unknown old jobs fail closed. Live submission rechecks retirement and states;
workers validate immutable retirement before work. No matching/model refit,
calibration repeat, mutation of old artifacts or confirmation/test access.

Unchanged workers publish BDZ_SHARD and BDZ_SELECTION with new stage lineage
and the reused original registry hash. Every artifact has canonical content
and parent hashes, schema version 1, final_test_accessed=false and immutable
publication. Statistical improvement is a result, never an execution gate.
