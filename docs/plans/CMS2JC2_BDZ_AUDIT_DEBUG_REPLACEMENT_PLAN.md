# Frozen B_DZ significance audit: debug replacement

Ryan authorized this execution-only replacement on 2026-09-26 after test-only
queue estimates favoured debug. This plan supersedes the tier3/no-cancellation
execution rules of CMS2JC2_BDZ_SIGNIFICANCE_AUDIT_PLAN.md only for the five
authenticated unfinished evaluation/report tasks of one original audit.
All scientific definitions and the development-only stop rule remain binding.

## Frozen inputs and acceptance

Use a fresh, disjoint study/root and clean pushed source. Import the original
BDZ_AUDIT_STAGE/v1, its fully acknowledged six-job ledger and submission
journals, and the completed ba_acceptance receipt/report. Preserve every
original artifact. Compare all donor scientific source bytes, including the
original bdz_audit_campaign/worker/metrics modules. Only the pre-existing
EXECUTION_ONLY_FILES allowlist may differ. No old artifact is reparented or
copied into the new root as if it had been generated there.

The new BDZ_AUDIT_DEBUG/v1 stage binds the original stage, ledger, acceptance,
acceptance receipt, frozen B_DZ comparison and the unchanged audit protocol.
Acceptance requires original COMPLETED/0:0 accounting, successful serial/process
replay, the registered sample count and finite measured resource projections
within 128 GiB/eight hours. Its two-worker measurement is reused for the same
36-process CPU-only execution profile; this is not a measured debug speedup.

Retain the same 10k development jets, four fixed shards, three replicas,
models/maps, PID groups, random keys, missingness, histograms, tail accounting,
forensic examples and report. No refit, reselection, confirmation, test or JC2
transfer is permitted. The new execution adapter calls the unchanged audit
evaluation kernel and report primitives. Report lineage references the original
acceptance hash and the four NEW shard hashes; it does not require an invented
new acceptance result. Poor physics remains a result, not an execution failure.

## New DAG

Stage bdz_audit_debug_r1, CPU-only debug, account reu-aisocial, qos_tier3,
atlas_kd_sporc, one node/task and no GPU:

- ba_eval_0..3: 36 CPUs, 128 GiB, eight hours each; no old scheduler dependencies.
- ba_report: one CPU, 32 GiB, four hours; afterok all four NEW evaluations.

Acceptance is authenticated reuse, not a sixth new job. The frozen evaluation
and report contracts remain BDZ_AUDIT_SHARD/REPORT v1. Debug start-time estimates
are not promises, and all four shards need not run concurrently.

## Safe retirement and submission

Creation/dry-run never cancel or submit. Import all acknowledged original IDs
from the supplied ledger, never from a job-name search or hard-coded ID range.
Preview the exact five targets. Reject a subject with evaluation/report claims
or completed output receipts. Require an additional exact retirement phrase
and reviewed plan hash before live retirement.

Check debug admission with sbatch --test-only before any cancellation. Preserve
all RUNNING, COMPLETING, SUSPENDED, successful or unknown old targets. For pending
targets check owner, task name, source worktree/script, campaign comment, CPU-only
allocation, tier3/account/QOS, and original submission intent. Use only one
controller-filtered scancel --ctld --state=PENDING with those exact target IDs,
tier3 and account filters. Never cancel acceptance or unrelated jobs. A race
to running is protected by the controller filter; stop if any target remains
active or accounting has not reached non-success terminal states. Recheck claims
and receipts after cancellation. Do not auto-retry or erase ambiguous evidence.

Publish immutable BDZ_AUDIT_DEBUG_RETIREMENT/v1 only after all old targets are
non-success terminal. It binds stage/plan/old ledger and each exact task/job.
Every live submit rechecks accounting; workers check the retirement and accepted
source artifacts before claiming work. Explicit normal submission phrase and
plan-hash review remain necessary. Interrupted acknowledgements use the existing
intent/receipt reconciliation, not a blind retry or new root.

The queue helper defaults to dry-run and prints phase-start/heartbeat messages
during expensive authentication. --execute separately authorizes retirement and
submission. No automatic Git commit/push, SSH or Slurm mutation by the agent.

## Verification and handoff

Test five-job shapes/dependencies, wrong-family/source/acceptance/ledger
rejection, claim/output preservation, admission-before-cancel, pending/running
races, accounting lag, exact-ID isolation, ambiguous and idempotent submission,
and byte-preserved donor artifacts. Exercise real synthetic ROOT evaluation and
reporting with the same scientific payload as the original audit. Real SPORC
acceptance reuse and pending states must be checked at execution, not inferred
from pasted job names. No Weaver/GPU validation applies to this CPU-only change.
